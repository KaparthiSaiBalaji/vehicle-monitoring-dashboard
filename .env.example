# Set to "false" once a real backend is running to switch off mock data
VITE_USE_MOCK=true

# Base URL of the Express backend (see the original project's API reference)
VITE_API_BASE_URL=http://localhost:5000/api
